New folder dialog is not working properly.
Typing in the input field doesn't work.

It makes more sense that the focus is on the input field when the dialog opens.


## overall layout improvement

can you investigate if we can move the object lists (like documents, ideas, etc.) to the left sidebar? As an expandable menu item under "Objects" or something like that.
With a "+" button to add new items to the list.
With a search bar to search for items in the list.
With a filter bar to filter items in the list.
With a trash can button to delete items from the list.
With a refresh button to refresh the list.
